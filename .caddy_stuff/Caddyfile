localhost {
    # Send all API requests to your hidden backend container (port 5000 or whatever port your app uses)
    handle /api/* {
        reverse_proxy backend:5000
    }

    # Send everything else to your frontend container
    handle {
        reverse_proxy frontend:5173
    }
}
